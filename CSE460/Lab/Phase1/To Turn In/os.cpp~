#include <iostream>

using namespace std;

        class VirtualMachine{
	private:
	  int msize;
	  int rsize;
	  int pc, ir, sr, sp, clock;
	  vector<int> mem;
	  vector<int> r;
	  int base, limit;
	  

	public:
	  VirtualMAchine(): msize(256), rsize(4), clock(0){
	    
	  }
			  
        };

	int main();{
	
	}
